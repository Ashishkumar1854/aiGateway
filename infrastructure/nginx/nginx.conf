# Nginx config — Phase 1
# This file is a placeholder. Full nginx.conf created in Phase 1 (Docker setup).

# Local development routing:
# aigw.local        → public-web  (:3000)
# app.aigw.local    → client-dash (:3001)
# admin.aigw.local  → admin-dash  (:3002)
# api.aigw.local    → backend     (:5000)
